<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>심부름 마켓</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">심부름 마켓</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item"><a class="nav-link active" aria-current="page" href="#">Home</a></li>
              <%if (status=="loggedIn"){%>
              <li class="nav-item"><a class="nav-link" href="/profile">내정보</a></li>
              <li class="nav-item"><a class="nav-link" href="/logout">로그아웃</a></li>
              <%}else{%>
              <li class="nav-item"><a class="nav-link" href="/login">로그인</a></li>
              <li class="nav-item"><a class="nav-link" href="/register">회원가입</a></li>
              <%}%>
              <li class="nav-item"><a class="nav-link" href="/community">유저 커뮤니티</a></li>
            </ul>
          </div>
        </div>
      </nav>
      <div class="container my-5">
        <div class="card" style="width: 100%">
            <div class="card-body">
              <%if (status=="loggedIn"){%>
              <h5 class="card-title">환영합니다 [ <%= user.name%> ] 님</h5>
              <%}else{%>
              <h5 class="card-title">환영합니다</h5>
              <%}%>
              <h6 class="card-subtitle mb-2 text-muted">심부름 마켓</h6>
              <p class="card-text">심부름 대행 커뮤니티 사이트 심부름 마켓입니다</p>
                  <%if (status=="loggedIn"){%>
                  <button class="btn btn-primary"><a href="/profile" style="color: white; text-decoration: none;">내정보</a></button>
                  <button class="btn btn-primary"><a href="/logout" style="color: white; text-decoration: none;">로그아웃</a></button>
                  <%}else{%>
                  <button class="btn btn-primary"><a href="/login" style="color: white; text-decoration: none;">로그인</a></button>
                  <button class="btn btn-primary"><a href="/register" style="color: white; text-decoration: none;">회원가입</a></button>
                  <%}%>
            </div>
          </div>
      </div>
</body>
</html>